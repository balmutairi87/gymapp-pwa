# نتيجة فحص GitHub Pages

بتاريخ 2026-08-26، يعيد الرابط `https://balmutairi87.github.io/gymapp-pwa/` صفحة 404 من تطبيق React رغم أن `curl` يعيد HTML بحجم 368KB للصفحة وHTTP 200 للملفات. حالة GitHub Pages هي `built` ومصدرها فرع `gh-pages`، لكن المتصفح يعرض صفحة NotFound داخل التطبيق. السبب المرجح أن ملف `index.html` المنشور يحتوي على مسار تشغيل مختلف أو أن نسخة React الحالية لا تتطابق مع نسخة الفرع المنشورة، ويجب فحص محتوى JavaScript المنشور ومسار BASE_URL/التوجيه قبل اعتماد الرابط.

بعد إضافة حارس مسار مباشر وإعادة النشر، فتح الرابط مع `?v=3` في المتصفح أظهر التطبيق كاملًا، ما يؤكد أن 404 كان من نسخة JavaScript/كاش قديمة لا من غياب ملفات GitHub Pages. يلزم التأكد من الرابط دون query بعد تحديث Service Worker أو إبطال الكاش، ثم تثبيت نسخة نهائية.

فحص المتصفح أظهر Service Worker مفعّلًا من `sw.js` مع cache `gymapp-shell-v2` ويخدم bundle قديمًا (`index-hU2hKynC.js`) عند الرابط الأصلي، لذلك يظهر 404 قديم. الرابط مع `?v=3` يجلب bundle الجديد ويعرض التطبيق. الإصلاح النهائي يجب أن يغيّر عنوان تسجيل Service Worker إلى نسخة versioned مثل `sw.js?v=3` مع cache جديد حتى يتم تحديث عامل الخدمة، ثم اختبار الرابط الأصلي بعد تحديث الكاش.

بعد فتح `?v=4` بنجاح، عاد الرابط الأصلي إلى 404 في نفس المتصفح، ما يعني أن عامل الخدمة القديم لا يزال يخدم الطلب الأساسي من cache-first. يلزم إبطال cache القديم خارج التطبيق، لا الاكتفاء بتسجيل عامل خدمة جديد من داخل Home الذي لا يُنفّذ عند ظهور 404.

تم مسح Service Worker والكاش في جلسة المتصفح، لكن الرابط الأساسي بقي يعرض bundle قديمًا وصفحة 404، بينما `?v=4` يفتح التطبيق. هذا يدل على أن GitHub Pages/CDN يعيد HTML قديمًا للرابط الأساسي، وليس أن ملفات الفرع مفقودة. الحل الأكثر موثوقية هو نشر نسخة Pages تحت رابط جديد ثابت لا يعتمد على cache قديم، مثل مستودع باسم gymapp-live، أو استخدام رابط versioned ثابت.
